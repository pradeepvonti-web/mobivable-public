# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Env (secrets). NOTE: `.env.example`, `.env.development`, and `.env.production`
# are committed on purpose — they hold only public/test client tokens.
# `.env` and `.env.*.local` must never be committed (they hold real API keys).
.env
.env.local
.env.*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
.claude/

# Local debug artifacts (typecheck logs etc.)
tsc.out.txt

# Playwright
/test-results/
/playwright-report/
/playwright/.cache/
/tests/e2e/.auth/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
